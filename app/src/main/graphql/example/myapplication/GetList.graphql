query GetList($owners: String!) {
  repositoryOwner(login:$owners) {
    repositories(first: 50)
    {nodes{
        name
        owner {
          avatarUrl
          url
          login
        }
      }
    }
  }
}