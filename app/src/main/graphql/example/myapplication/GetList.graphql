query GetList{
    viewer {
        login
        repositories(first: 50)
        {nodes{
            name
            owner {
                avatarUrl
                url
                login
            }
        }
        }
    }
}