query GetList{
    viewer {
        login
        repositories(first: 25)
        {nodes{
            name
            owner {
                avatarUrl
                url
                login
            }
        }
        }
    }
}